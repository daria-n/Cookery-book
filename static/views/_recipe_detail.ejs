<div class="recipe">
    <div class="recipe-title">
        <h3>{{ detail.currentRecipe.name }}</h3>
        <img ng-src="{{ detail.buildPathToImg(detail.currentRecipe.UrlName) }}" alt="" width="100px">
    </div>
    <div class="recipe-content">
        <div>
            <strong>Trudność:</strong>
            <p>{{ detail.currentRecipe.difficulty }}</p>
        </div>
        <div>
            <strong>Czas wykonania:</strong>
            <p>{{ detail.currentRecipe.time }}</p>
        </div>
        <div>
            <strong>Składniki:</strong>
            <ul>
                <li ng-repeat="ingredient in detail.currentRecipe.ingredients">
                    <p>{{ ingredient }}</p>
                </li>
            </ul>
        </div>
        <div>
            <strong>Wykonanie:</strong>
            <ol>
                <li ng-repeat="step in detail.currentRecipe.procedure">
                    <p>{{ step }}</p>
                </li>
            </ol>
        </div>
    </div>
</div>
